# 2026-09-25 #287 고정 스쿱 무게 판정 마무리 — 팀장 대조 반영·D-35 C 몫 — C 공정 (Claude 세션)

## 목표
#287(#282 무한 보충 루프 수정)을 리뷰에 올릴 수 있는 상태로 만든다. B #272 결과, 팀장 정합성 대조, D-35 C 몫까지 같은 PR 에 반영한다.

## 시도와 결과 (시간순)

| 시각 (KST) | 시도 | 결과 | 배운 것 |
|---|---|---|---|
| 2026-09-25 19:18:04 (`3c68261` 커밋 시각) | #287 을 main(`d4eeb67`) 위로 다시 얹기. 이 세션 이전 작업이다 | 9/23 #289(A)와 겹치던 깊이 1.0·배선을 빼고, 접촉 우회와 무게 그물만 남겼다. main 과 충돌 0 | 인계 첫 확인 때는 옛 head(`02855f8`)를 보고 「충돌 있음」이라고 보고했다. **fetch 전에 판단했기 때문이다** — 원격 브랜치가 강제 푸시로 바뀌어 있었다 |
| 2026-09-25 ≈19:20 (< 19:25:53 `d5224b2`) | 인계 — C CURRENT, 미머지 브랜치, 열린 PR·이슈 대조 | main 에서는 #282 가 아직 살아 있다: `skill_node` 고정 경로가 늘 `contact_detected=False`, `process_fsm` 이 그 값을 빈 스쿱으로 읽는다 | 옛 head 끼리 합치면 `common.yaml` 이 충돌 표시 없이 합쳐지고 **`fixed_scoop:` 키가 두 번** 남았다. git 이 조용히 넘긴 곳이 제일 위험하다 |
| 2026-09-25 ≈19:20 (위와 같은 구간) | B 가 19:10~19:17 에 올린 #283 확인 — 스쿱 실측, 「`fixed_scoop` 끄기 철회」, 시연 최소 조건 | `3c68261` 코드 주석에 #272 값이 이미 들어가 있었다. C 쪽에는 결론만 옮기고 수치는 B CURRENT 로 링크했다(규칙 5) | |
| 2026-09-25 19:25:53 (`d5224b2`·`34b38f2`) | 자동 점검(9/23)이 짚은 문서 3곳에, 같은 문장을 가진 곳을 grep 으로 찾아 더했다 | `process_flow`·`architecture`·`interfaces`·`setup`·`SOT`·그림 생성기. 생성기를 다시 돌리니 라벨 4개만 바뀌었다 | 지적된 3곳을 넘어 **같은 문장을 grep** 해야 한다. 그래도 팀장 대조에서 더 나왔다(아래) |
| 2026-09-25 19:44:44 (팀장 코멘트 생성) | 팀장 정합성 대조 코멘트 수신 | 문서 누락을 더 짚었다: ASCII 흐름, drawio 7곳, T6, docstring, **DB 에 남는 `detail` 문자열**. CURRENT 대조 6건, D-35 C 몫 | 내 첫 보완이 **「후속 수정 필요」라는 문구만** 찾았다. 이 PR 이전부터 틀려 있던 라벨(「붓기 비율」, 투입 전 계량 무효 「→ QA」)은 그 grep 에 걸리지 않는다 |
| 2026-09-25 19:53:49 (`6be0c83`) | 빈 스쿱 일탈 `detail` 에 판정 증거를 남긴다 | 무게로 판정해도 「원료에 닿지 않는다」로 기록되던 것을 고쳤다. 접촉/순중량을 구분하고 순중량·문턱값을 함께 남긴다. 시험 1건 추가 | 문구도 기록이다. **판정 근거가 바뀌면 기록 문자열도 같이 바뀌어야 한다** — 9/23 의 「거동은 맞는데 기록이 틀린」 모양과 같다 |
| 2026-09-25 19:53:49 (`11a8893`) | 팀장 목록의 문서를 전부 반영 | SOT·setup 「B/C 인계」 2번은 앞 커밋에서 옛 문장을 지웠던 것을 되살려 **취소선과 철회 표시**로 바꿨다 | 규칙 3(틀린 결론은 지우지 않는다)이 CURRENT·일지뿐 아니라 **SOT 의 서술 절에도** 적용됐다. 팀장이 명시하기 전에는 나는 지웠다 |
| 2026-09-25 19:54:55 (`95652ee`) → 푸시 | C CURRENT 대조 반영 | 네 곳의 출처(#274 / A #289 / #287), 「133 이면 소싱 누락」 철회, 규칙 5 링크화, `empty_scoop_g` 두 뜻 함정 | ROS 없이 돌려도 150 passed / 2 skipped 가 나온다. **숫자로 소싱 누락을 가릴 수 없다** |
| 2026-09-25 19:56:30 (`e1fcd83`, 로컬) | D-35 레시피 85→79·170→158 을 별도 브랜치에 커밋 | 비 ROS 시험은 통과했다. D `test_v4_recipes.py` 는 의도대로 실패(사본과 동시 머지 조건) | **나중에 버렸다** — 아래 21:19 행 |
| 2026-09-25 ≈19:56 (< 19:56:30 커밋) | `process_node` 선언 기본값 변경 시도 | **보류.** ROS 시험이 가짜 스킬 노드의 공칭 40 g 과 이 기본값을 짝으로 가정한다. ROS 설치는 막혀 있다(packages.ros.org 403, CI 없음) | 검증할 수 없는 거동 변경은 올리지 않는다. 그렇게 보고했다 |
| 2026-09-25 19:56:48 (PR 코멘트) | #287 에 반영 결과 보고 | | |
| 2026-09-25 21:19:50 (팀장 코멘트 수정 시각) | 팀장 코멘트 수정본 확인 | **운영 레시피는 C 가 아니라 D 담당**(SOT D-35 `cc9b444`, 사용자 확인). C 몫은 `process_node` 기본값 하나 | `interfaces.md` §4 의 「recipes 담당 C」만 보고 레시피를 C 몫으로 읽었다. 그 줄 자체가 낡은 계약 문서였다. **담당 판단은 최신 SOT 결정으로 한다** |
| 2026-09-25 ≈21:23 (< 21:24:00 `72e8eaa`) | 레시피 브랜치 폐기 | 원격에 올린 적이 없어 로컬 브랜치와 워크트리만 지웠다 | 푸시 전에 사용자에게 물어본 덕분에 D 영역에 흔적이 남지 않았다 |
| 2026-09-25 21:24:00 (`72e8eaa`) | C CURRENT 정정 | 「C 몫 ① 레시피」에 철회 표시를 달았다. 근거를 #284 로 바꾸고 머지 순서를 「#287 → D 레시피·사본 PR」로 고쳤다 | |
| 2026-09-25 21:25:03 (`aca519c`) → 푸시 | `process_node` 기본값 79 / 0.10 / 0.1 | ROS 시험 두 곳이 옛 값(40·0.15·0.5)을 `parameter_overrides` 로 **명시**하게 해서 시험 거동을 고정했다. 비 ROS 시험 185 passed / 2 skipped | 기본값에 기대는 시험은 기본값이 바뀌면 조용히 거동이 달라진다. **시험이 전제하는 값은 시험 안에 적는다** |

## 오늘 확정된 것
- **고정 모드에서는 접촉을 안 보고 순중량으로 빈 스쿱을 가른다.** 무게 그물은 모드와 무관하게 늘 돈다. 계량 무효는 빈 스쿱으로 보지 않는다. — PR #287 `3c68261`, SOT D-34(#284)
- **빈 스쿱 일탈 `detail` 에 판정 증거를 남긴다**(접촉 / 순중량·문턱). — `6be0c83`, `test_빈_스쿱_기록은_어느_증거로_판정했는지를_남긴다`
- **`process_node` 선언 기본값 = 운영값**: `scoop_nominal_g` 79, `min_fraction` 0.10, `scale.zero_drift_limit_n` 0.1. 런치는 `common.yaml` 을 넘기므로 운영 거동은 그대로다. — `aca519c`, SOT D-35
- **운영 레시피 `params/recipes/*.yaml` 은 D 담당이다.** C 에는 스키마·검증(`recipe.py`)만 남는다. — SOT D-35 `cc9b444`
- 문서 동기화: `process_flow.md`·`architecture.md`·`interfaces.md` 운용 주석·`setup.md`·`SOT.md` 「B/C 인계」·`demo_run_procedure.md` T6·그림 생성기. — `d5224b2`·`11a8893`

## 철회한 것
- ~~「#287 은 main 과 충돌한다」(인계 첫 보고)~~ → 19:18 에 이미 다시 얹혀 충돌이 없었다. 옛 head 를 보고 판단했다. **판단 전에 fetch 한다.**
- ~~SOT·setup 「B/C 인계」 2번을 새 문단으로 교체(`d5224b2`)~~ → 옛 문장을 취소선으로 남기고 철회 표시를 달았다(`11a8893`, 팀장 지시).
- ~~「D-35 C 몫 ① 운영 레시피 목표값」(`95652ee` CURRENT, 로컬 `e1fcd83`)~~ → 운영 레시피는 D 담당이다(SOT D-35 `cc9b444`). 커밋은 푸시 전에 버렸다.
- ~~「`process_node` 기본값은 ROS 없이는 못 바꾼다」(19:56 보류)~~ → 시험이 옛 값을 명시하게 하면 시험 거동이 바뀌지 않아 안전하게 바꿀 수 있다(`aca519c`). **다만 ROS 시험의 실제 재실행은 여전히 안 됐다.**

## 남은 것 · 다음 세션이 먼저 읽을 것
- ⚠️ **ROS 환경에서 `test_process_node`·`test_run_batch_ros` 를 재실행해야 한다.** 이 세션에는 ROS 가 없었다. 기준선은 201/6 에 오늘 시험 1건이 더해져 **202/6 예상**이다. 다르면 `aca519c` 의 `parameter_overrides` 부터 본다.
- #287 은 D 리뷰(교차검수)를 기다린다. 머지 순서: **#287 → D 레시피·사본 PR**. B 의 `common.yaml` `scoop_nominal_g` 79 는 B PR 에서 바뀐다.
- C 자체 후속: `test_process_fsm.py` 에 박힌 85·170 을 cfg·레시피에서 읽게 한다. D 레시피가 79·158 로 바뀌면 낡는다.
- `dosing.empty_scoop_g` 2.0 은 잠정값이다. 빈 스쿱 계량 산포는 아직 아무도 재지 않았다(#272 는 퍼낸 양의 산포).
- `interfaces.md` §4 의 「recipes 담당 C」는 팀장이 팀 공지 뒤 고친다. **그 줄을 근거로 레시피를 고치지 말 것.**
