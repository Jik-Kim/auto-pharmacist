# 2026-09-23 v1.8 INVALID 소비 · DONE_UNMEASURED 기록 경로 — D aszx4880-star (Claude 세션)

## 목표
계약 v1.8 `DispenseResult.verdict=INVALID` 와 SOT D-32 `DONE_UNMEASURED` 가 DB·HMI 에 빠짐없이 남게 한다.

## 전날 이어받은 것 (9/22, 오늘 일지의 전제)
| 시각 | 시도 | 결과 | 배운 것 |
|---|---|---|---|
| 2026-09-22 시각 미기록 (< 21:48, #229 머지 전) | 공유 도메인 70 에서 주문 제출 → 배치가 바로 끝남. 처음엔 스쿠핑 `calibrated:false` 게이트로 진단 | **틀림** — 이벤트 로그가 `BATCH_CANCELLED` 였고 우리 HMI 의 `BATCH_CANCEL_REQUEST` 감사 기록이 없었다. 격리 도메인 91 에서 정상 | 즉시 종료는 **로그의 종료 코드부터** 본다. 외부 취소는 감사 기록 유무로 가린다 |
| 2026-09-22 시각 미기록 (< 21:48) | 기록·통계 탭 로그 영역 확장 1차: 높이 상한만 올림 | 사용자 반려 — 셀 크기는 그대로, **카드 안 빈 공간**을 쓰라는 요구 | 요구는 「셀」이 아니라 「스크롤 영역」이었다 |
| 2026-09-22 시각 미기록 (< 21:48) | 2차: `height:fit-content` | 세 카드 하단이 계단식으로 어긋남 | 숫자 상수 대신 **구조**로 풀어야 한다 |
| 2026-09-22 21:48 | 3차: `body` flex + `main` flex:1 + `#records` grid 4행 `1fr` | 머지(PR #229). 뷰포트를 채우고 세 카드 하단 정렬 | `body overflow:hidden` 이 **모든 폭**에 걸린다 — 모바일 확인 필요(아래 남은 것) |

## 시도와 결과 (시간순, 9/23)
| 시각 | 시도 | 결과 | 배운 것 |
|---|---|---|---|
| 2026-09-23 11:55 | #108 D 몫: `VERDICTS[3]='INVALID'`, 배지 INVALID → bad (PR #240 머지) | C #241 과 **D 먼저** 순서로 11:57 합류 | 소비자가 열거값을 모르면 DB 에 `'3'`, 화면에 `'?'` — 계약 상수 추가도 소비자 동시 머지가 필요 |
| 2026-09-23 11:57 | #241 리뷰·승인 (C 발행) | 승인 1명으로 머지됨 — 계약은 **최소 2명** 규칙 | 사후 승인 받기로 함 |
| 2026-09-23 12:36 | #242 진행 스트립 INVALID 「완료」 표시 | 다른 세션이 PR #245 로 해소 | D 열린 과제는 머지 로그로 먼저 확인할 것 |
| 2026-09-23 ≈15:35 (< 15:41 커밋) | D-32 D 몫 구현 — `has_unmeasured`·`reconcile_unmeasured`, `reconcile_discard` IN, KPI 두 지표 | test_db +5 통과, gmp_hmi 전체는 main 기준선과 실패·오류 동일(통과 +5). 변이 검사: `reconcile_discard` 를 옛 조건으로 되돌리면 새 시험이 깨짐 | 이벤트 순서가 보장되지 않으면 **종료 시 판정 + 종료 뒤 UPDATE** 두 길을 다 둔다. 판정은 메모리가 아니라 DB 로 — 재시작에 강하다 |
| 2026-09-23 15:41 | 커밋 f0b77ba, PR #261 생성, #257 에 준비 완료 코멘트 | — | — |
| 2026-09-23 15:48 · 15:49 · 15:50 | #261 → #257 → #256 순서로 머지 | 순서 지켜짐 | — |

## 오늘 확정된 것
- `VERDICTS` 3 = INVALID, 배지 bad — PR #240
- 미측정 배치 기록: 이벤트/INVALID 근거 + 종료 뒤 UPDATE 흡수, 폐기 대사 IN — PR #261
- KPI: 계량 검증 완료율(DONE) / 미측정 승인 완료 / 완주율 — PR #261, SOT D-32

## 철회한 것
- ~~배치 즉시 종료 = calibrated 게이트~~ → 공유 도메인 외부 취소 (9/22, 격리 도메인 재현)
- ~~KPI 는 DONE_UNMEASURED 「제외」만~~ → 두 지표 분리 (SOT D-32)

## 남은 것 · 다음 세션이 먼저 읽을 것
- `practice/D/CURRENT.md` 부터.
- **#261 통합 확인(미검증)**: 로컬 셀에서 미측정 배치 → DB `batches.result='DONE_UNMEASURED'`, KPI 카드 「미측정 승인 N건」, 결과 필터.
- **G6 휴대폰 리허설(미검증)**: `body overflow:hidden` 하에서 모바일 스크롤·버튼.
- `docs/interfaces.md:188` KPI 이름 — 조장에게 전달.
