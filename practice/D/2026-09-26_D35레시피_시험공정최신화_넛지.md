# 2026-09-26 D-35 레시피 · 시험 공정 최신화(넛지·보충 대기) · 발표 자료 — D aszx4880-star (Claude 세션)

휴강 중(9/24~28). 실물 없이 레시피 값을 D-35 에 맞추고, 통신 시험용 가짜 공정이 실제 공정과 같은 말을 하게 만들었다.
시각은 KST. 커밋 시각은 컨테이너(UTC) 기록에 9 시간을 더했다. 사용자 PC 실행은 사용자가 붙인 로그 시각이다.

## 목표
레시피를 D-35(79/158 g)로 바꾸고, `/hmi_test` 시험 공정을 실제 공정 흐름(넛지·보충 대기·단계 순서)에 맞춰 실제 DDS 로 검증한다.

## 시도와 결과 (시간순, KST)
| 시각 | 시도 | 결과 | 배운 것 |
|---|---|---|---|
| 13:24 | #284(SOT D-34·D-35) 검토 코멘트 | 레시피 담당 D·D-35 분담은 맞음. `docs/interfaces.md:174` 「`SubmitOrder` 로 보낸다」 → `RunBatch` 로 고쳐 달라(반영되면 승인). 「D-35 PR 은 #287 머지 뒤」라고 적음 | — |
| 13:56 → 14:01 | 운영 레시피 79/158 g + 시험 사본 + 검증기·launch·데모·문서, PR #291 | 사용자 결정(「그냥 올려」)으로 #287 머지 **전에** 올리고 본문 맨 위에 머지 순서 표(#284 → #283 → #287 → #291)를 둠. 세 PR 과 충돌 없음 | 올리는 시점과 머지 시점은 다르다 — 순서는 본문에 적어 두면 된다 |
| ≈14:10–14:25 (< 14:26 점검 알림) | 발표 자료용 HMI·DB 기능 전수 목록 | `index.html`·`hmi.js`·`hmi_web_node.py`·`record_node.py`·`db.py` 에서 화면 카드·팝업 9종·권한·DB 테이블 10개·감사 조작 20가지·KPI 5개 정리 | 목록은 코드에서 뽑는다 — 기억으로 쓰면 빠진다 |
| ≈14:30–15:25 (14:26 < · < 15:26 점검 알림) | 1분 영상 장면 구성 | 정상 레시피·QA 원격 승인·부족 알람·넛지·기록 화면. 부족 팝업이 **주문 차단**까지 하는 것은 시험·데모 재고뿐(`hmi.js:78`), 실물은 C 의 REFILL 정지 흐름 | 시연 장면은 어느 모드에서 찍었는지 자막에 남긴다 |
| ≈19:2x (< 19:36 커밋) | 집 PC 에서 #291 DDS 재검증 | ① 저장소가 없어 `cd` 실패 → 명령이 다른 워크스페이스 `~/ros2_ws` 에서 빌드되다 실패(피해 없음) ② clone 후 `tools/env.sh` 대신 `/opt/ros/jazzy` + `--packages-up-to gmp_hmi` ③ 검증기 「초기 재고 필요」 FAIL — 브라우저에서 주문을 먼저 넣음 ④ launch 새로 → **20/20 PASS** | 집 PC 에는 두산 언더레이가 없다 — HMI 시험은 그게 없어도 된다. 검증기는 **새 launch 직후, 화면을 건드리기 전에** |
| 19:36 | #291 본문·CURRENT 에 20/20 PASS | `e35b723` | — |
| ≈19:40 | 「시험 중에 넛지 구현됐어?」 | 실제 공정(A `core/nudge.py`·C `process_fsm` NUDGE_WAIT·`process_node._on_event`)엔 있으나 **시험 공정엔 없음**. 20 PASS 는 넛지를 검증하지 않았다 | PASS 목록에 없는 기능은 검증되지 않은 것이다 |
| ≈19:45 → 20:01 | 시험 공정을 실제 흐름에 맞춰 다시 짬(사용자 「전부」 선택) | 실제 `process_fsm`·`process_node`(main + #287) 대조 중 발견: ① 원료 소진이 실제는 **QA 가 아니라** 자동 재시도 3회 → REFILL 보충 대기 ② 옛 시험 공정은 RunBatch 결과를 **시작 1초 뒤** 돌려줘 「현재 배치 중단」이 한 번도 안 켜짐 ③ 취소 단계 `ABORTED` 가 hmi.js 사전에 없어 영문 노출 ④ PAUSED 팝업 제목이 사유와 무관하게 「공정 일시 정지」 ⑤ 실제 C 는 **QA 폐기 판정 즉시 DONE** 을 내고 그 뒤에 반송·NUDGE_WAIT(record_node 가 첫 DONE 에 배치를 닫음) ⑥ `verify_hmi_controls_ros.py` 목표 120 g 이 옛 레시피 값. pytest 131 → **140 passed**, 프런트 2종 PASS. 커밋 4개(`14fa729`·`0b28cc3`·`2e9192d`·`fc174ac`) | 시험 대역은 실제 코드를 기준으로 **주기적으로 다시 대조**해야 한다 — 9/21 이후 C 가 바꾼 흐름을 대역이 못 따라갔다 |
| 00:04 (9/27) | 집 PC 에서 새 검증기 실제 DDS | **22/22 PASS**(배치 9건). 넛지는 `ros2 topic pub --once -w 3` 로 한 번만 — 두 번 가면 토글이 되돌아간다 | — |
| 00:07 → 00:08 (9/27) | CURRENT 22/22 반영(`f2512cb`), PR #295(base = `feature/d35-recipes-79`) | #291 위에 쌓아 기준을 #291 브랜치로 — 이 PR 커밋 5개만 보이게 | — |
| ≈00:10 (9/27) | 휴대폰으로 `http://192.168.0.11:5002/` | 열리지 않음(데스크탑 유선). 원인 미확인 — 같은 공유기 와이파이·`ufw`·AP 격리 중 하나. 화요일 교육장에서 확인하기로 | — |

## 오늘 확정된 것
- 운영 레시피 D-35 79/158 g ±10 % + 시험 사본·검증기, 실제 DDS 20/20 PASS — PR #291
- 시험 공정 = 실제 공정 흐름(단계 순서·NUDGE_WAIT·운전 중/유휴 넛지·원료 소진 REFILL·Goal 유지·고정 스쿱 기록), 검증기 22 항목 실제 DDS 22/22 PASS, pytest 140 — PR #295
- HMI: 취소 단계 「배치 중단」, 정지 사유별 팝업 제목 — PR #295 `14fa729`
- C 합의 필요: QA 폐기 직후 DONE — `ros2_ws/src/gmp_hmi/docs/interface_alignment.md` 「종료」 행

## 철회한 것
- ~~시험 시나리오 `material_empty` = MATERIAL_EMPTY → QA 판정(9/23 PR #266)~~ → 실제 C 는 QA 가 아니라 SCOOP_EMPTY 자동 재시도 3회 → MATERIAL_EMPTY 보충 대기(`AUTO_RECOVERED`)다(`gmp_process/core/deviation.py` `RULES`). #266 이 정책표를 확인하지 않고 일탈 종류만 맞췄다. PR #295 로 정정.
- ~~통신 검증 20 PASS = 통합 전 D 경로 검증 완료~~ → 넛지·보충 대기·배치 중단 버튼은 시험 공정이 **내지 않아서** 검사되지 않았다. PR #295 의 22 항목부터 포함.
- ~~스쿱 계량 위치 `workbench`(검증기 기대값)~~ → 실제 `process_node` 는 `material_N`(`WeighHeld` 자세). PR #295.

## 남은 것 · 다음 세션이 먼저 읽을 것
- `practice/D/CURRENT.md` 부터. CURRENT 의 D-35·22 항목 줄은 #291·#295 브랜치에 있다 — **머지돼야** main 에 들어간다.
- 머지 순서: #284 → #283 → #287 → #291 → #295. #291 머지 뒤 #295 의 base 를 main 으로.
- #284 에 요청한 `interfaces.md:174` 수정이 반영되면 승인.
- C 에 전달: QA 폐기 직후 DONE(위). 합의 전까지 실물에서 폐기 배치는 반송 중에 기록이 먼저 닫힌다.
- 휴대폰 확인(G6) — 화요일 교육장. 안 열리면 같은 네트워크 → `curl http://<PC IP>:5002/auth/session` → `sudo ufw allow 5002/tcp` 순서.
- 영상의 넛지 장면은 실물로만 찍힌다 — A 에게 실물 넛지(`passbox_done → nudge_wait`) 확인 여부부터 묻기(`practice/A/CURRENT.md` 에 미검증).
