# 2026-09-23 통신 검증 최신화 · 통합 전 정리 · 휴대폰 화면 — D aszx4880-star (Claude 세션)

같은 날 `2026-09-23_표시정합성_문서정정_가상모드검증.md` 를 쓴 세션의 **저녁 몫**이다. 그 일지의 「남은 것」 중
스쿠핑 실측 보고는 팀 회의·SOT D-33(#268)로 넘어갔고, `SubmitOrder` 주석·`docs/interfaces.md` KPI 이름은 조장에게 전달했다.

## 목표
통합(실물 `/cell`) 전에 D 쪽에서 막힐 것을 없앤다 — 시험 도구가 최신 계약을 실제로 검사하게, 시험·데모 화면이
실제 공정과 같은 말을 하게, 휴대폰(G6)에서 쓸 수 있게.

## 시도와 결과 (시간순, KST — 커밋·PR·코멘트 시각)
| 시각 | 시도 | 결과 | 배운 것 |
|---|---|---|---|
| 18:00 → 18:05 | 통신 검증기·시험 공정을 v1.8·D-32 까지 확장(17 → 20 항목), PR #266 | 사용자 환경 실제 DDS **20/20 PASS**. 시험 노드가 9/20 이후 `INVALID`·`BATCH_UNMEASURED`·`DONE_UNMEASURED`·`MATERIAL_EMPTY` 를 **발행조차 안 했다** — 17 PASS 는 새 기능의 근거가 아니었다 | 「PASS」는 시험이 그 값을 내보낼 때만 의미가 있다. 도구의 마지막 수정일부터 본다 |
| 18:15 → 18:17 | `tools/test_safety_popup.cjs` 살릴지 판단, PR #267 | 팝업 로직이 아니라 **DOM 스텁**이 9/22 `f349fe2`(`appendChild` 추가)를 못 따라가 터졌다. 스텁 한 줄로 PASS, `robot_state` 허용 목록을 일부러 넓히면 실패 — 가드 17 건 유효 | 시험이 깨졌다고 시험을 지우지 않는다. 먼저 「무엇이 틀렸나」를 가른다 |
| 18:48 → 18:50 | 조장 검토: #266 이 `test_v4_process.py` 1 건을 **결정적으로** 깨뜨림 | 손으로 적은 스텁 상수에 `BATCH_OUT_OF_SPEC` 등이 없었고 값도 계약과 달랐다(`OVERFILL` 0/1). 상수를 `.msg` 에서 읽게 고쳐 126 passed | 스텁 상수는 손으로 적지 말고 계약 파일에서 읽는다 |
| 19:22 | #266·#267 머지 | — | — |
| ≈19:25 | D-33(#268·#271) 확인 — 운영 레시피 85/170 g ±10 % | #271 은 운영 레시피만 바꾸고 `gmp_hmi` 시험 사본·데모·통신 검증기는 40/80 g ±5 % 그대로. `test_v4_recipes.py` 는 **사본끼리만** 비교해 못 잡았다 | 사본이 있으면 원본과 대조하는 시험이 있어야 한다 |
| 19:37 → 19:40 | 사본·데모·검증기를 D-33 에 맞추고 사본↔운영 대조 시험 추가, PR #276 | overfill 배율 1.10 은 ±10 % 에서 경계값이라 1.15 로. 시작 재고 A 170 g 으로 부족 거부 재현 유지 | 허용오차가 바뀌면 「과다」 시나리오 배율도 같이 봐야 한다 |
| 19:50 | 사용자 환경 실제 DDS 재실행 | **20/20 PASS**(D-33 값). 첫 시도는 launch·검증기 `GMP_HMI_ADMIN_PASSWORD` 가 달라 401 | 두 터미널 비밀번호가 같아야 한다 → CURRENT 함정 |
| 19:56 | #276 머지 | — | — |
| 20:00 → 20:13 | 통합 전 전수 대조(PR #280) | 시험 launch 가 새 시나리오를 거부(#266 누락) · 시험·데모 단계 이름 7 개가 steps 맵에 없어 영문 노출 · 안 읽히는 `demo_batch.yaml` · 쓰지 않는 `POST /collection-confirm`(SOT D-23 문구 함께 정정) · `LEVELS` 사본. 대조 시험 4 건 추가, 131 passed | 계약·이름표는 사본이 생기는 순간 어긋날 준비가 된다 — 한 곳에서 가져오거나 대조 시험으로 묶는다 |
| ≈20:15 | 실제 공정과 맞물리는 부분 대조 | 토픽·서비스·액션 이름·QoS, 메시지 필드, 안전 이벤트 JSON 키, `RunBatch` 필드, 상태 주기 0.5 s 대 신선도 3 s, 옛 `cell.db` 이관 — **어긋남 없음** | — |
| 20:26 | 시연 절차 T1 에 기록 DB 새로 시작 단계 | 그대로 두면 리허설·시험 배치가 KPI·이력에 섞인다 | — |
| 20:29 → 20:38 | 사용자 휴대폰 실물 확인 → 수정 반복 | 고정 헤더가 화면 30~50 % → 페이지 스크롤, 안내띠만 고정 · 안전 복구 버튼 65×25 → 44px · 기록·통계가 390px 에서 417px 로 넓어짐 → `main` 을 화면 폭에 묶음 · 「종료 미만」 → 「종료」 · 날짜 칸이 실물에서만 가로로 벗어남 → `appearance:none` | headless Chromium 의 모바일 폭 ≠ 실제 휴대폰 브라우저. **날짜 입력은 실물로 봐야 한다** |

## 오늘 확정된 것
- 통신 검증 20 항목, v1.8·D-32·D-33 기준 실제 DDS PASS — PR #266·#276
- 안전 복구 팝업 시험 복구 — PR #267
- 시험 사본 ↔ 운영 레시피 대조 시험 — PR #276
- 통합 전 정리·계약 대조 시험 4 건·휴대폰 화면 — PR #280 (리뷰 대기)

## 철회한 것
- ~~#266 본문 「`gmp_hmi` pytest 55 passed — 기준선 회귀 없음」~~ → **틀렸다.** `PYTHONPATH` 없이 돌려 69 건이 import ERROR 로 빠졌고 그 안에 #266 이 깨뜨린 시험이 있었다(조장 검토 18:48). 본문 정정, CURRENT 함정에 올바른 실행법. #267 본문의 같은 숫자도 정정.
- ~~휴대폰 기록·통계 날짜 칸 「화면 안」(Chromium 390·360px 확인)~~ → 실물 휴대폰에서는 벗어났다(20:3x). 브라우저 기본 모양이 폭을 무시한다. `f5524f8` 로 수정, iOS Safari 는 여전히 실물 확인 필요.
- ~~휴대폰 헤더 버튼 같은 폭 2×2(`c6e7427`)~~ → 사용자 의견으로 되돌림(`af9d037`). 로그아웃만 상태 줄 오른쪽.

## 남은 것 · 다음 세션이 먼저 읽을 것
- `practice/D/CURRENT.md` 부터. **#280 이 머지돼야** CURRENT 의 기준선(131)·레이아웃 줄이 main 에 들어간다.
- #231 에 D 몫(레시피 사본·HMI 표시·검증) 완료 코멘트 — #276 로 끝났다.
- 실물 통합: G6 휴대폰 리허설(iOS 날짜 칸 포함), #261 실제 `/cell` 기록, #136 정지 사유 표시, #133 중단 버튼 C 연동, #128.
- #139 1 분 영상·PPT (조장과).
