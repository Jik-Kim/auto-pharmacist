# 2026-09-23 표시 정합성 · 문서 정정 · 가상 모드 완주 검증 — D aszx4880-star (Claude 세션)

같은 날 `2026-09-23_v1.8-INVALID_DONE_UNMEASURED_기록.md` 와 **다른 세션**이다. 그쪽은 v1.8 판정값의
기록 경로(D-32)를, 이 세션은 표시 정합성·문서 정정·가상 모드 검증을 맡았다. 겹친 것은 #245 하나이고
그쪽 일지에도 「다른 세션이 PR #245 로 해소」로 적혀 있다.

## 목표
화면이 공정 상태를 **거짓 없이** 보이게 하고, 문서가 코드와 다른 말을 하지 않게 한다.

## 시도와 결과 (시간순, KST)
| 시각 | 시도 | 결과 | 배운 것 |
|---|---|---|---|
| 10:19 → 10:21 | `hmi.js steps` 맵을 `process_fsm` 상태와 대조 (PR #232) | `NUDGE_WAIT`·`CLEANUP` **누락**, 공정이 안 내는 죽은 키 5개(`WEIGH`·`VERIFY_FINAL`·`DISCARD_FINISH`·`WAIT_QA`·`INTERLOCK`). 양방향 대조 시험 신설 후 머지 | 사전에 없는 상태는 화면에 **영문 원시 문자열**로 나온다. `NUDGE_WAIT` 은 정상 완료마다 반드시 지나가므로 시연에서 바로 보였을 것 |
| ≈10:37 | 최신 main 으로 `cell.launch.py mode:=virtual` 실행, `run_cell_cycle.py`(개인 PC 스크립트)로 주문→상태→QA→기록 한 바퀴 시도 | 로그인 **HTTP 403** | 모든 POST 는 `X-CSRF-Token` 이 필요하다(`hmi_web_node.py:839~`). `/auth/session` 으로 토큰을 먼저 받고, 로그인이 세션을 비우며 재발급하므로 응답의 `csrf_token` 을 매번 갱신해야 한다 |
| 10:42 → 10:43 | CSRF 고치고 재실행 | 주문 수락 → `RUNNING`/`PICK_CONTAINER` → **`GRIP_FAIL` ×4 → ERROR**. 일탈 4건(자동 재시도 3 + FORCED 1) DB 기록 확인 | HMI 오류 경로는 정상 동작. 막힌 것은 그 앞단 |
| ≈10:50 | `GRIP_FAIL` 원인 추적 | `rg2_gripper.py:246` 의 파지 판정 `grip = w > width_mm + grip_margin_mm`. `virtual` 백엔드(`:175`)는 명령한 관절각으로 **그대로** 이동하므로 `w ≈ width_mm`, 마진 2.0 mm(`common.yaml:61`)를 넘을 수 없다 → **항상 거짓** | 가상 모드로는 `PICK_CONTAINER` 를 원리적으로 통과 못 한다. `modbus` 에는 `_native_grip` 경로가 있지만 `virtual` 에는 없다 |
| ≈10:55 | 세트 끝 `NUDGE_WAIT` 도 확인 | `skill_node.py:126` 이 `scale.simulated` 면 NUDGE 감지를 끄는데, `process_node` 의 `safety.nudge_enabled` 는 `true` 그대로(`common.yaml:118`)이고 `_await` 에 타임아웃이 없다(`:315-318`) → **무한 대기** | 파지를 넘겨도 세트 끝에서 멈춘다. `/cell/event` 에 `code='NUDGE'` 를 한 번 발행하면 파라미터 변경 없이 풀린다(사람이 건드린 것과 같은 경로) |
| 11:27 → 11:54 | `gmp_hmi` 문서 전수 대조 (PR #237) | 문서 5개에 **거짓 주장 9건**. 대표: 「잔량 카드·개별 보충 버튼은 운영/시험/데모 모두 비표시」인데 실제로는 렌더됨 | `bafb9b4`(9/22 잔량 카드 복구)가 코드만 고치고 문서를 안 고쳤다. 그 커밋은 `remaining_controls.md` 를 **읽고 인용까지** 했는데 그 문서가 거짓이 된다는 데까지 가지 못했다 |
| 11:36 → 11:55 | 사문화 코드 제거 (PR #238) | `db.py active_batch()`(호출부 0건, `restart_records` 가 같은 질의 포함)·`hmi.js lastHttpError`·`reportRows`·`recoveryPhase`(대입만)·제거된 체크박스 CSS 2줄·참조 없는 id 2개 | `recent_weights`·`pending_deviations` 는 **남겼다** — 시험이 DB 저장을 검증하는 데 쓴다. 「운영 경로 미사용」과 「죽은 코드」는 다르다 |
| 12:23 → 12:37 | #242 진행 스트립 (PR #245) | `complete` 가 `devs.some(APPROVED)` 를 보는데 INVALID 는 **정의상 항상 APPROVED** 를 갖는다 → 초록 「완료」. 배지는 같은 원료를 bad(빨강)로 그려 한 화면이 서로 다른 말을 했다 | 도달 불가였던 `'재계량'` 가지도 지웠다. 회귀 시험은 playwright 없이 도는 **정적 대조**로 넣었다 |
| ≈16:05 (< 16:13 세션 갱신) | 사용자가 **저울로 직접** 계량 — 한 스쿱 순량 **80~90 g**(원료 꽉 찬 상태, 로봇 계량 아님) | 설정과 크게 다름. `dosing.scoop_nominal_g` 40.0 / `scooping.A` 기준 순량 65(=100−35) / 실측 85 — **세 값이 전부 다르다** | `_first_fraction` = `target/scoop_nominal` = 40/40 = **1.0**(만땅) → 85 g 퍼올림 → 목표 초과 → `RETURN_MATERIAL` 강제. 실물은 반환→재스쿱 경로가 아직 없어(#64) 거기서 ERROR |

## 오늘 확정된 것
- `steps` 맵 ↔ FSM 상태 1:1, 양방향 회귀 시험 — PR #232
- `gmp_hmi` 문서 5개 정정(거짓 9건) — PR #237
- 사문화 코드 제거 — PR #238
- 진행 스트립 INVALID 「투입량 미확인」 — PR #245 (#242 종료)

## 철회한 것
- ~~T7 「HMI 에서 SafePose」를 우리가 구현 안 했다~~ → **있다**. 「진입·복귀 요청」 ENTER 가 `process_node.py:543` 에서 `SafePose` 를 부른다. `gmp_hmi` 안에서만 `SafePose` 문자열을 찾다가 잘못 결론냈다. HMI 가 로봇을 직접 부르지 않는 것이 오히려 설계(AGENTS.md 「로봇을 만지는 노드는 skill_node 하나」)다.
- ~~`gmp_hmi/docs/*` 는 조장 소관이라 못 고친다~~ → `ros2_ws/src/gmp_hmi/docs/` 는 **우리 패키지 안**이다. 저장소 루트 `docs/` 와 혼동했다.

## 남은 것 · 다음 세션이 먼저 읽을 것
- `practice/D/CURRENT.md` 부터. 이 세션이 「알려진 함정」에 가상 모드 2건을 더했다.
- **스쿠핑 실측 85 g 을 #231 에 보고** — 아직 아무 데도 기록 없음. `scoop_nominal_g`(B)·`scooping.A` 프로필(A) 둘 다 우리가 못 고친다.
- `empty_scoop_g` 파일 간 불일치 — `stations.yaml:71` **35.0** vs `gmp_dosing/config/scale_reference.yaml:175` **32.0**. 순량 계산에 직접 들어간다.
- `gmp_interfaces/srv/SubmitOrder.srv:1`·`msg/Recipe.msg:1` 이 「HMI 가 SubmitOrder 로 보낸다」고 적었으나 HMI 는 `RunBatch` 액션을 쓴다. 계약 문서라 조장 판단.
