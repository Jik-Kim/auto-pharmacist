# B 도징·계량 현행 상태 (갱신: 2026-09-23)

**담당**: nomal-npc + 김병직 (대행, 담당자 토큰 소진, 9/23~)

> 세션 시작 때 이 파일을 읽는다. 값을 쓰기 전에 근거 링크의 원본을 직접 연다.

## 지금 유효한 값
| 항목 | 값 | 근거 |
|---|---|---|
| 공구 설정 `tool_weight` (펜던트, **동결**) | 1.36 kg · CoG [5.31, −34.68, 8.28] mm (PLA 부착물, 자동측정 3회 평균) | PR #235 `scale_reference.yaml` retracted_0923 |
| 런타임 계량 `scale.gain / offset_g / max_std_g` | 0.8859 / 247.091 / 10.0 — **우드락 공구(9/19~21) 조건 값, PLA 공구에서 미검증** | `ros2_ws/src/gmp_bringup/params/common.yaml:81-83` |
| 용기 경로 gain (PLA, 미확정) | ≈ 1.076 (한 파지 2점, 229 g 유효 1회) | PR #235 본문 · #187 |
| 로봇 재파지 σ (용기 229 g, 8파지 12분, 추세 제거) | **6.99 g** — 한 파지 유지는 0.85 g | #187 9/23 정정 코멘트 |
| VERIFY ① 허용폭 Σ(target×tol) | recipe-01/02/03 = **6 / 6 / 8 g** (총 120/120/160 g, tol 5 %) | `params/recipes/recipe-0*.yaml`, `process_fsm.py:441` |
| VERIFY ① 성립 여부 | **불성립** — σ_순량 √2×6.99 ≈ 9.9 g > 허용폭, 재파지만으로 통과 ≈ 46 % | SOT D-26 9/23 추기(PR #239), 조장 판단 대기 |
| 계량 무효 재시도 | `max_invalid_retries: 2` (총 3회 측정) | #213 결정 1, PR #225 |
| 스쿱 1회량 `scoop_nominal_g` | 40.0 (common.yaml) — #216 은 65 g, 불일치 | #221 (B 몫: 65 로 올리려면 decide() 하한 조정 동반) |
| 영점 이동 한계 `zero_drift_limit_n` | 0.1 N | common.yaml:84 |

## 열린 과제 (이슈 번호)
- #187 재파지 σ·`max_std_g` 확정 — 범위 되돌림(9/23). PLA 공구 gain 확정: 로봇 가용 시 **한 파지 안** 79/79/229/229 재측정(≈2분). 다른 파지 점을 섞지 말 것(섞으면 1.39 같은 값이 나온다).
- #221 `scoop_nominal_g` 40→65 + `decide()` 하한(교착 구간: 최소채취 > 2×허용오차) + #210 `max_attempts` 하한.
- #208 고주파 게이트 `max_hf_std_g` 배선(A) → #219 리베이스.
- 조장 판단: ① 불성립 대응(재파지 σ 저감 / tol·목표량 조정 #53 / ① 보류), common.yaml:34 공구 주석(두 세대 전 값).
- 원인 미상: workbench 자세 Mx ≈ +0.9 Nm, material_1/2 계량 σ 4.3~5.7 g·무효율 50 %(A 인계).

## 알려진 함정
- **레시피가 바뀌었다.** 9/22 이전 문서·계산의 「A/B/C = 200/150/100 g, 허용폭 22.5 g」은 폐기값. `README.md:83` 에 아직 남아 있다(후속 정정).
- 한 파지 반복성(σ ≈ 1 g)을 재파지 반복성(σ ≈ 7 g)으로 읽지 말 것. TARE 와 VERIFY 는 다른 파지다.
- 공구 자동측정은 cz 를 「틀리게」 내는 게 아니라 원래 약한 성분이라 흔들린다(변동계수 24.5 %). 동결값을 임의로 바꾸지 않는다.
- gmp_hmi 와 gmp_process 시험을 같은 실행에 넣으면 노드 경합으로 `test_scoop_cycle_attempt_numbers_are_unique_per_material` 이 깨진다. 패키지별로 나눠 돌린다.

## 철회 이력 (최근 것 위)
- 2026-09-23 ~~cz = 89 mm (실측 확정)~~ → 무게중심이 아니라 1.079 Nm 보정항. 동결값 8.28. PR #235.
- 2026-09-23 ~~「자동측정을 돌리면 2.32 로 덮이므로 돌리지 말 것」~~ → 철회. PR #235, demo T0(PR #239).
- 2026-09-23 ~~로봇 재파지 σ_cup 0.91 g~~ → 한 파지 유지값. 실제 재파지 6.99 g. #187.
- 2026-09-23 ~~① 허용폭 22.5 g, 오검 ≈ 2 %~~ → 6/6/8 g, 통과 ≈ 46 %. PR #239·#235 5183d3e.
- 2026-09-22 ~~VERIFY ②(계측 신뢰성 대조)~~ → 폐지, ① 만 판정. SOT D-26, PR #209.
