# 2026-09-25 B 인계 · 스쿱 실측일 정정 · D-35(79 g) 반영 · 팀장 대조 — B 도징 (Jik-Kim 대행 세션 2, Claude)

## 목표
대행 세션 1(9/23~25)이 남긴 #272 스쿱 실측 결과를 인계받아 기록을 바로잡고, D-35 B 몫과 팀장 대조(PR #283 코멘트)를 반영한다.

## 시도와 결과 (시간순)
시각은 세션 생성·커밋·코멘트 시각(KST). 세션 생성 18:58.

| 시각 | 시도 | 결과 | 배운 것 |
|---|---|---|---|
| ≈ 18:58–19:05 (세션 생성 뒤, 첫 질의 전) | 인계 — CURRENT(9/23 판)·`gmp_dosing`·열린 이슈·PR 대조 | CURRENT 이후 main 에 도징 변경 3건(`e301cc9`·`e62b8da`·`cc61c9a`, 고정 스쿱). `gmp_dosing` main 35 passed | CURRENT 가 이틀 낡아 있었다 — 값은 yaml·코드 원본으로 확인했다 |
| ≈ 19:05–19:10 (19:10 커밋 전) | 「85 g 테스트 적용됐나」 확인 | PR #283(`feature/scoop-sigma-272`) 미머지·리뷰 0. main 에 합쳐 48 passed. 실측 원본은 저장소·이슈 어디에도 없었다 | 기록 도구가 반환·재스쿱을 모의하지 않는다(아래) |
| 19:10–19:17 | (대행 세션 1) 실측 원본 CSV·결과·B·C 측정 도구 커밋 `9023840`…`4ff3fac` | 평균 78.9 · σ 3.90 · 상한 5.69 재현 확인. 스크립트 출력 = CURRENT 표 | 같은 브랜치를 두 세션이 동시에 고치고 있었다 — 푸시 거절 1회, 내 미푸시 커밋만 재배치 |
| 19:18 | 실측일·저장소 커밋 정정 `6e84a3b` | 측정은 9/23(사용자 확인). 파일명·시험명·CURRENT 의 9/24 → 9/23, CSV 머리말을 「1회차 5b658e0 · 2~15회차 #290 뒤」로 | 첫 시도에서 `git mv` 뒤 내용 수정을 스테이징하지 않아 이름만 바뀐 커밋이 생겼다 → 푸시 전이라 로컬에서 다시 나눴다 |
| 19:18 | 시연 명령 `vel_scale:=1.0` `a46574f` | `demo_run_procedure.md` T1. 런치 기본값 0.3·`common.yaml` 은 유지(SOT 9/23 「런치 기본 배율 유지」, 사용자 선택) | 측정 조건과 시연 조건이 문서로 이어져야 측정값이 시연을 대표한다 |
| 19:45 | 팀장 대조 코멘트(PR #283) | D-35 (가) 채택, B 몫 + CURRENT 정정 목록 | — |
| 19:52 | D-35 B 몫 `3343fdf` (main 병합 `47214ae` 뒤) | `scoop_nominal_g`·`DosingConfig` 79, 고정 스쿱 시험 79·158 재계산, `scoop_sigma.py` nominal 79·레시피를 nominal 에서 생성, `max_invalid_retries` 주석 정정. dosing 52 · process 144 passed | 시험 경계값은 새 목표에서 다시 계산해야 한다(보충 가능 상한 94.8, 부분 깊이 구간 79.1 이상) |
| 19:53–19:55 | CURRENT 절 단위 4커밋 `97f2db6`·`a5a735e`·`cce068c`·`06c2559` | 줄 번호·규칙 5·8·#221·#249 전제·철회 8건 | 「8 배」는 근거를 찾지 못해 철회로 적었다 |
| 21:20 | 팀장 코멘트 수정(운영 레시피 담당 **D**) + 19:59 후속(#249 닫힘) | 내가 C 로 적은 곳 3군데·#249 「결정 대기」가 틀려졌다 | 코멘트는 올라온 뒤에도 고쳐진다 — 반영 전 다시 읽는다 |
| 21:25 | 정정 `06f4207` + PR 정정 댓글 | 담당 D, #249 설계는 철회 표시 후 참고 설계로 접음(사용자 선택), 근거 PR #284 | — |

### 인계 중 따로 계산한 것 (스크립트 미반영)
- `scoop_sigma.py` 완주율은 **넘친 스쿱을 곧바로 QA 로 센다.** 실제 공정은 붓기 전 스쿱 계량에서 넘치면 반환·재스쿱한다(`process_fsm.py` WEIGH_SCOOP, `max_returns` 3).
- 같은 15회로 반환을 넣어 모의(부트스트랩 20000회, 퍼올림≈투입량 근사): 목표 79 에서 세 레시피 **≈ 99.9 %**(반환 없음 81.0·92.8·86.8 %). 목표 85 는 넘치는 회차가 없어 21 % 그대로.
- 반환 판단은 PLA 공구에서 미보정인 로봇 스쿱 계량에 기대므로 실제는 82 % 와 99.9 % 사이로 CURRENT 에 적었다.

## 오늘 확정된 것
- 스쿱 실측(#272) 측정일 **9/23**, `vel_scale` **1.0**, 2~15회차는 #290 뒤 — `calibration/scoop_sigma_0923_matA.csv`
- `dosing.scoop_nominal_g` · `DosingConfig` 기본값 **79.0** — SOT D-35(PR #284), 커밋 `3343fdf`
- 시연은 `vel_scale:=1.0` — `docs/demo_run_procedure.md` T1, `a46574f`
- 운영 레시피 파일 담당 **D** — SOT D-35 `cc9b444`
- #249 닫힘, 현 재파지 반복성으로 시연 — SOT D-31 9/25 결정(`c2fd498`)

## 철회한 것
- ~~스쿱 측정일 9/24~~ → 9/23 (사용자 확인). `6e84a3b`
- ~~운영 레시피 담당 C (interfaces.md §4)~~ → D. 팀장 코멘트 수정본. 커밋 `3343fdf` 메시지와 19:56 PR 댓글의 「C」는 PR 댓글로 정정(이력은 다시 쓰지 않음)
- ~~#249 합격 기준 조장 결정 대기~~ → 9/25 닫힘. `06f4207`
- 그 밖 CURRENT 철회 8건은 CURRENT 「철회 이력」 9/25 줄

## 남은 것 · 다음 세션이 먼저 읽을 것
- **PR #283 리뷰 0건** — 리뷰어 A, `common.yaml` 은 조장 파일이라 조장 승인도 필요
- **원료 B·C 1회량 측정(최우선)** — 절차는 CURRENT. 79 ± 5 g 밖이면 원료별 기준값 재결정(SOT D-35)
- D-35 C 몫(`process_node` 기본값)·D 몫(운영 레시피+사본 한 PR)은 각 담당. 운영 레시피가 79 로 바뀌기 전까지 `common.yaml` 79 와 레시피 85 가 공존한다(교착 조건은 둘 다 성립, 시험 통과)
- #187 용기 경로 gain 3점 측정, #219 main 충돌
- `scoop_sigma.py` 완주율에 반환·재스쿱 모의를 넣을지 — 위 계산 참고
- 같은 브랜치를 다른 세션이 동시에 고칠 수 있다 — 작업 전 `git fetch`, 푸시 거절 시 자기 미푸시 커밋만 재배치
